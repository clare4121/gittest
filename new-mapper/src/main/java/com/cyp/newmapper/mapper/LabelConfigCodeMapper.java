package com.cyp.newmapper.mapper;

import com.cyp.newmapper.entity.LabelConfigCode;

public interface LabelConfigCodeMapper {
    int insert(LabelConfigCode record);

    int insertSelective(LabelConfigCode record);
}